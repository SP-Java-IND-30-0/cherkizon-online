package com.github.spjavaind300.commentsservice.dto;

/**
 * Enum representing the roles a user can have within the system.
 * Used for access control and authorization.
 */
public enum Role {
    ADMIN, USER, SERVICE
}